[project]
name = "project-akrav"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cartopy>=0.25.0",
    "matplotlib>=3.10.8",
    "networkx>=3.6.1",
    "numpy>=2.4.1",
    "pandas>=2.3.3",
    "scipy>=1.17.0",
    "seaborn>=0.13.2",
    "shapely>=2.1.2",
]

[tool.uv.workspace]
members = [
    ".",
]

[tool.uv.sources]
project-akrav = { workspace = true, editable = true }

[dependency-groups]
dev = [
    "project-akrav",
]
